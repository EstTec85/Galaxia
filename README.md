# Galaxia Proyecto

This project runs the `galaxia.py` script, which catalogs a low bright system image folder. Follow the steps below to set up and execute the script.

## Prerequisites

- Python 3.8 or higher
- `pip` (Python package manager)
- Git (optional, for cloning the repository)

## Installation

1. **Clone the repository (if applicable):**
   ```bash
   git clone https://github.com/EstTec85/Galaxia
   cd Galaxia